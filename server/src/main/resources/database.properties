# Configuration base de données - ChriOnline
# Base utilisée : PostgreSQL

# PostgreSQL
db.url=jdbc:postgresql://localhost:5432/chrionline
db.user=postgres
db.password=

# Driver PostgreSQL (à ajouter dans pom.xml : postgresql)
# <dependency>
#   <groupId>org.postgresql</groupId>
#   <artifactId>postgresql</artifactId>
#   <version>42.7.1</version>
# </dependency>

# --- Ancienne config MySQL (décommenter si besoin) ---
# db.url=jdbc:mysql://localhost:3306/chrionline
# db.user=root
# db.password=
